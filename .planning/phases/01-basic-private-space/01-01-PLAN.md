---
phase: 01-basic-private-space
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - nuxt.config.ts
  - assets/css/main.css
  - components/ui/button/Button.vue
  - components/ui/card/Card.vue
  - components/ui/input/Input.vue
  - components/ui/label/Label.vue
  - components/ui/alert/Alert.vue
  - components/ui/badge/Badge.vue
  - components/ui/skeleton/Skeleton.vue
  - pages/login.vue
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - DASH-01
user_setup: []
must_haves:
  truths:
    - "AUTH-01: Sign-in remains email/password based and authenticated users can reach the private app after login."
    - "AUTH-02: Auth state persists across refresh and logout remains available."
    - "DASH-01: Dashboard remains protected by existing global middleware after UI setup."
  artifacts:
    - "shadcn-vue/Tailwind configuration exists in package.json, package-lock.json, nuxt.config.ts, and assets/css/main.css."
    - "Local shadcn-vue components exist for Button, Card, Input, Label, Alert, Badge, and Skeleton."
    - "pages/login.vue uses the Phase 1 UI contract primary CTA text: Sign in to space."
  key_links:
    - "pages/login.vue form -> useFirebaseAuth.login -> navigateTo('/dashboard')"
    - "nuxt.config.ts CSS/modules -> assets/css/main.css -> shadcn-vue components"
---

<objective>
Establish the Phase 1 shadcn-vue app shell and preserve the working auth entry path.

Purpose: This plan gives every later Phase 1 UI task a consistent component system while keeping the already-working Firebase login and route guard intact.
Output: shadcn-vue/Tailwind setup, approved local UI components, and an updated login screen using the UI-SPEC copy and states.
</objective>

<execution_context>
@/Users/mekari/Documents/ChatAPP/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mekari/Documents/ChatAPP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-basic-private-space/01-CONTEXT.md
@.planning/phases/01-basic-private-space/01-RESEARCH.md
@.planning/phases/01-basic-private-space/01-UI-SPEC.md
@package.json
@nuxt.config.ts
@assets/css/main.css
@pages/login.vue
@composables/useFirebaseAuth.ts
@middleware/auth.global.ts
</context>

<threat_model>
Assets: Firebase Auth session, partner email, private app route access, UI trust boundary.
Threats: accidental auth regression, route guard bypass, raw Firebase auth errors exposed in UI, malicious third-party UI registry code.
Mitigations planned: use shadcn-vue official components only; do not add third-party registry blocks; preserve `useFirebaseAuth.login`, `initAuthListener`, and `middleware/auth.global.ts`; keep user-facing login errors concise and avoid raw exception details.
Block on: any implementation that disables auth middleware, changes route protection semantics, or adds third-party UI blocks without registry vetting.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn-vue for Nuxt</name>
  <files>package.json, package-lock.json, nuxt.config.ts, assets/css/main.css, components/ui/button/Button.vue, components/ui/card/Card.vue, components/ui/input/Input.vue, components/ui/label/Label.vue, components/ui/alert/Alert.vue, components/ui/badge/Badge.vue, components/ui/skeleton/Skeleton.vue</files>
  <read_first>package.json, package-lock.json, nuxt.config.ts, assets/css/main.css, .planning/phases/01-basic-private-space/01-UI-SPEC.md</read_first>
  <action>Install and configure shadcn-vue for this Nuxt 3 app per the UI-SPEC: add Tailwind/shadcn-vue Nuxt support using npm-compatible commands, add only the official `Button`, `Card`, `Input`, `Label`, `Alert`, `Badge`, and `Skeleton` components, and configure theme tokens so #f8f5f1 is the page background, #ffffff is the card/surface color, #2f4d4f is the primary/focus color, #9b4d45 is the support accent, #687174 is muted text, #d8d1ca is border, and #9f2d28 is destructive. Do not add React shadcn/ui packages. Do not add Dialog, Sheet, Drawer, Toast/Sonner, Sidebar, Calendar, icon dependencies, or third-party registry blocks. This implements the user's shadcn preference while preserving D-09 dashboard tone and D-20 static/simple visual direction.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains shadcn-vue/Tailwind Nuxt dependencies or scripts needed by the installed shadcn-vue setup.
    - nuxt.config.ts contains the configuration required for the installed Tailwind/shadcn-vue setup and preserves the Firebase runtimeConfig keys.
    - components/ui contains Button, Card, Input, Label, Alert, Badge, and Skeleton component files.
    - No component files for Dialog, Sheet, Drawer, Toast/Sonner, Sidebar, Calendar, or third-party registry blocks are added.
    - assets/css/main.css contains the Phase 1 color values #f8f5f1, #2f4d4f, #9b4d45, #687174, #d8d1ca, and #9f2d28.
  </acceptance_criteria>
  <done>shadcn-vue official components are installed locally, theme tokens match the UI-SPEC, and build/typecheck still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate login to the Phase 1 component contract</name>
  <files>pages/login.vue, assets/css/main.css</files>
  <read_first>pages/login.vue, composables/useFirebaseAuth.ts, middleware/auth.global.ts, assets/css/main.css, .planning/phases/01-basic-private-space/01-UI-SPEC.md</read_first>
  <action>Update `pages/login.vue` to use the local shadcn-vue `Card`, `Input`, `Label`, `Alert`, and `Button` components while preserving the existing `email`, `password`, `isSubmitting`, `login(email, password)`, password clearing on failed login, and `navigateTo('/dashboard')` behavior. Replace the submit button text with `Sign in to space` when idle and keep a submitting state that does not resize the button. Keep `/login` as the only public route through the existing global middleware. This covers AUTH-01, AUTH-02, and D-16 by keeping sign-in functional without exposing raw Firebase details in new UI copy.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - pages/login.vue contains the exact idle CTA string `Sign in to space`.
    - pages/login.vue still calls `await login(email.value, password.value)` and `await navigateTo('/dashboard')`.
    - pages/login.vue does not contain the generic submit label `Submit`.
    - middleware/auth.global.ts is not changed by this task.
  </acceptance_criteria>
  <done>Login remains functional, uses the approved component contract, and continues to route authenticated users to `/dashboard`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes.
- [ ] `npm run build` passes.
- [ ] `rg -n "Sign in to space" pages/login.vue` returns at least one match.
- [ ] `rg -n "Dialog|Sheet|Drawer|Sonner|Calendar" components/ui` returns no Phase 1 component additions.
</verification>

<success_criteria>

- shadcn-vue official components are available for subsequent Phase 1 plans.
- Login still uses Firebase Auth and protected-route navigation.
- No third-party UI registry code enters the repository.
- AUTH-01, AUTH-02, DASH-01, D-09, D-16, and D-20 remain satisfied by the app shell.
</success_criteria>

<artifacts_this_phase_produces>
- components/ui/button/Button.vue
- components/ui/card/Card.vue
- components/ui/input/Input.vue
- components/ui/label/Label.vue
- components/ui/alert/Alert.vue
- components/ui/badge/Badge.vue
- components/ui/skeleton/Skeleton.vue
- shadcn-vue/Tailwind Nuxt configuration in nuxt.config.ts
- Phase 1 theme tokens in assets/css/main.css
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/01-basic-private-space/01-01-SUMMARY.md`.
</output>
