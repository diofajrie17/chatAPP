---
phase: 01-basic-private-space
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - pages/dashboard.vue
  - components/AnniversaryCountdown.vue
  - components/FeatureCard.vue
  - assets/css/main.css
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - SPCE-03
  - DASH-01
  - DASH-02
user_setup: []
must_haves:
  truths:
    - "SPCE-03: Dashboard shows the configured couple name for both members."
    - "DASH-02: Dashboard shows anniversary countdown and configured anniversary date."
    - "D-09/D-10: Dashboard reads as a private home overview with couple name, countdown, date, and Chat/Notes/Album preview cards."
    - "D-11: Chat/Notes/Album preview cards are disabled and do not navigate."
    - "D-12: Dashboard header includes a visible Settings button near Log out."
    - "D-13/D-14/D-15/D-16: Access-error panels are calm and hide technical details."
  artifacts:
    - "pages/dashboard.vue renders shadcn-vue dashboard cards, Settings action, Log out action, and access-error panels."
    - "components/AnniversaryCountdown.vue displays the configured date and a countdown for valid YYYY-MM-DD input."
    - "components/FeatureCard.vue renders disabled preview cards for Chat, Notes, and Album."
  key_links:
    - "pages/dashboard.vue -> useCoupleSpace.loadCoupleSpace -> couple.displayName and couple.anniversaryDate"
    - "pages/dashboard.vue Settings button -> navigateTo('/settings')"
    - "useCoupleSpace accessErrorCode -> dashboard calm access-error copy"
---

<objective>
Finish the private dashboard experience and calm access-error UI.

Purpose: This plan turns the authenticated page into the private home overview promised in Phase 1 and gives users clear non-technical outcomes when setup is incomplete or access is denied.
Output: dashboard Settings action, three disabled feature previews, anniversary display, and calm access-error panels.
</objective>

<execution_context>
@/Users/mekari/Documents/ChatAPP/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mekari/Documents/ChatAPP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-basic-private-space/01-CONTEXT.md
@.planning/phases/01-basic-private-space/01-RESEARCH.md
@.planning/phases/01-basic-private-space/01-UI-SPEC.md
@.planning/phases/01-basic-private-space/01-02-SUMMARY.md
@pages/dashboard.vue
@components/AnniversaryCountdown.vue
@components/FeatureCard.vue
@composables/useCoupleSpace.ts
@composables/useFirebaseAuth.ts
@assets/css/main.css
</context>

<threat_model>
Assets: private couple identity, membership status, setup diagnostics, authenticated route state.
Threats: leaking Firebase paths/UIDs/errors to users, clickable previews implying unavailable routes, non-member confusion, accidental loss of logout path from denied/error states.
Mitigations planned: render access-error categories through approved calm copy; keep raw details in console only; include logout on access-error panels; ensure disabled feature cards do not navigate; preserve global auth middleware and logout action.
Block on: any dashboard copy that shows raw exception text, Firestore document paths, UIDs, or unavailable feature navigation.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild dashboard header, private overview, and disabled previews</name>
  <files>pages/dashboard.vue, components/FeatureCard.vue, assets/css/main.css</files>
  <read_first>pages/dashboard.vue, components/FeatureCard.vue, composables/useCoupleSpace.ts, composables/useFirebaseAuth.ts, .planning/phases/01-basic-private-space/01-UI-SPEC.md</read_first>
  <action>Update `pages/dashboard.vue` to use shadcn-vue `Card`, `Badge`, `Skeleton`, and `Button` components from Plan 01. Keep the couple name as the page identity, render signed-in account copy, add a visible `Settings` button beside `Log out` that navigates to `/settings`, and keep logout wired through `useFirebaseAuth.logout`. Render exactly three disabled preview cards: Chat, Notes, Album. Remove the current fourth Memories card and ensure previews have no click handler, no NuxtLink, and no placeholder route. Update `FeatureCard.vue` only as needed so it is explicitly disabled/presentational. This implements D-09, D-10, D-11, and D-12 while preserving AUTH-01, AUTH-02, and DASH-01.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - pages/dashboard.vue contains exact visible action text `Settings`.
    - pages/dashboard.vue contains exact visible action text `Log out`.
    - pages/dashboard.vue contains feature card titles `Chat`, `Notes`, and `Album`.
    - pages/dashboard.vue and components/FeatureCard.vue do not contain `Memories`.
    - Feature preview cards do not use `NuxtLink` or `navigateTo` for Chat, Notes, or Album.
  </acceptance_criteria>
  <done>The dashboard header and feature previews match the private-home Phase 1 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Display anniversary countdown and configured date cleanly</name>
  <files>components/AnniversaryCountdown.vue, pages/dashboard.vue, assets/css/main.css</files>
  <read_first>components/AnniversaryCountdown.vue, pages/dashboard.vue, types/couple.ts, .planning/phases/01-basic-private-space/01-UI-SPEC.md</read_first>
  <action>Update `AnniversaryCountdown.vue` and its dashboard usage so valid `YYYY-MM-DD` input shows both the day countdown and the configured anniversary date. Replace setup-console fallback copy such as `Set in Firestore` and `Add anniversaryDate as YYYY-MM-DD.` with calm private-home copy that does not expose implementation details. Keep the calculation based on the next anniversary date and preserve singular/plural day text. This implements DASH-02 and D-10.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - components/AnniversaryCountdown.vue does not contain `Set in Firestore`.
    - components/AnniversaryCountdown.vue does not contain `Add anniversaryDate as YYYY-MM-DD.`
    - components/AnniversaryCountdown.vue still formats dates with `Intl.DateTimeFormat`.
    - components/AnniversaryCountdown.vue still renders `day`/`days` based on the computed countdown.
  </acceptance_criteria>
  <done>The dashboard displays anniversary countdown and date without setup-console wording.</done>
</task>

<task type="auto">
  <name>Task 3: Render calm access-error panels</name>
  <files>pages/dashboard.vue, assets/css/main.css</files>
  <read_first>pages/dashboard.vue, composables/useCoupleSpace.ts, composables/useFirebaseAuth.ts, .planning/phases/01-basic-private-space/01-CONTEXT.md, .planning/phases/01-basic-private-space/01-UI-SPEC.md</read_first>
  <action>Replace the raw `{{ error }}` danger panel in `pages/dashboard.vue` with shadcn-vue `Alert`/`Card` panels mapped from `accessErrorCode`. Use exact headings and bodies from UI-SPEC: `This account is not configured yet`, `Your private space setup is incomplete`, `Private space unavailable`, and `We could not load your private space`. Include a `Log out` button in error panels. Do not render Firestore paths, exception messages, UIDs, document IDs, or rule details. This implements D-13, D-14, D-15, and D-16 while preserving AUTH-03 and AUTH-04 denial clarity.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - pages/dashboard.vue contains `This account is not configured yet`.
    - pages/dashboard.vue contains `Your private space setup is incomplete`.
    - pages/dashboard.vue contains `Private space unavailable`.
    - pages/dashboard.vue contains `We could not load your private space`.
    - pages/dashboard.vue does not render raw `{{ error }}` in the user-facing template.
    - pages/dashboard.vue error-state markup includes a logout action.
  </acceptance_criteria>
  <done>All configured access failure modes render calm, actionable, non-technical UI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes.
- [ ] `npm run build` passes.
- [ ] Manual browser check shows dashboard has Settings and Log out actions.
- [ ] Manual browser check shows exactly Chat, Notes, and Album as disabled previews.
- [ ] Manual setup-error checks show calm copy and no raw Firebase path or exception text.
</verification>

<success_criteria>

- Dashboard shows couple name, anniversary countdown, configured date, and exactly three disabled feature previews.
- Settings is visible near Log out and navigates to `/settings`.
- Unknown or misconfigured access cases show calm, non-technical copy.
- AUTH-01, AUTH-02, AUTH-03, AUTH-04, SPCE-03, DASH-01, DASH-02, and D-09 through D-16 are covered.
</success_criteria>

<artifacts_this_phase_produces>
- Dashboard access-error view model mapping in pages/dashboard.vue
- Disabled Phase 1 feature preview card presentation
- Settings navigation action from dashboard
- Anniversary countdown/date display copy
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/01-basic-private-space/01-03-SUMMARY.md`.
</output>
