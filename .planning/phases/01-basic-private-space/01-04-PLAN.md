---
phase: 01-basic-private-space
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - nuxt.config.ts
  - public/manifest.webmanifest
  - public/icons/icon-192.png
  - public/icons/icon-512.png
  - README.md
autonomous: true
requirements:
  - PWA-01
  - REPO-01
  - AUTH-01
  - AUTH-02
  - DASH-01
user_setup: []
must_haves:
  truths:
    - "PWA-01: App has install metadata baseline for Add to Home Screen testing."
    - "REPO-01: Local git origin fetch and push URLs are `https://github.com/diofajrie17/chatAPP.git`."
    - "D-17/D-18/D-19/D-20/D-21: PWA scope is manifest/icons/theme metadata only, named Our Private Space, with no service worker or offline shell."
    - "D-22/D-23: Phase 1 verifies the GitHub origin and does not push unless explicitly requested."
  artifacts:
    - "public/manifest.webmanifest exists with name `Our Private Space`."
    - "public/icons/icon-192.png and public/icons/icon-512.png exist."
    - "nuxt.config.ts links manifest and theme color metadata."
    - "README.md documents manual Add to Home Screen and git remote verification."
  key_links:
    - "nuxt.config.ts app head -> public/manifest.webmanifest"
    - "public/manifest.webmanifest icons -> public/icons/icon-192.png and public/icons/icon-512.png"
    - "README.md verification steps -> `git remote -v` output expectation"
---

<objective>
Add install metadata and close Phase 1 with local repository verification.

Purpose: This plan gives the private app the PWA metadata baseline requested for Add to Home Screen testing and records the local GitHub origin connection without pushing.
Output: manifest, static icons, Nuxt head metadata, README verification instructions, and git remote verification.
</objective>

<execution_context>
@/Users/mekari/Documents/ChatAPP/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mekari/Documents/ChatAPP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-basic-private-space/01-CONTEXT.md
@.planning/phases/01-basic-private-space/01-RESEARCH.md
@.planning/phases/01-basic-private-space/01-UI-SPEC.md
@nuxt.config.ts
@README.md
@.git/config
</context>

<threat_model>
Assets: app install identity, private-space branding, repository remote configuration.
Threats: adding service worker/offline behavior that implies unsupported privacy guarantees, leaking personal imagery in icons, pushing local code without explicit request, documenting misleading install or security claims.
Mitigations planned: manifest/icons/theme metadata only; simple generated static icons with approved colors; README manual install check; `git remote -v` verification only; no service worker, no push, no personal imagery.
Block on: adding offline shell/service worker behavior, adding personal photos to icons, or pushing to GitHub during Phase 1 execution.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add PWA manifest and static icons</name>
  <files>nuxt.config.ts, public/manifest.webmanifest, public/icons/icon-192.png, public/icons/icon-512.png</files>
  <read_first>nuxt.config.ts, assets/css/main.css, .planning/phases/01-basic-private-space/01-CONTEXT.md, .planning/phases/01-basic-private-space/01-RESEARCH.md, .planning/phases/01-basic-private-space/01-UI-SPEC.md</read_first>
  <action>Create `public/manifest.webmanifest` with `name` set to `Our Private Space`, a short name suitable for mobile, `start_url` set to `/dashboard`, `scope` set to `/`, `display` set to `standalone`, `background_color` set to `#f8f5f1`, `theme_color` set to `#2f4d4f`, and icon entries for `/icons/icon-192.png` and `/icons/icon-512.png`. Generate simple static PNG icons using #f8f5f1 background, #2f4d4f primary mark, and #9b4d45 support detail; do not use personal/couple imagery. Update `nuxt.config.ts` app head metadata to link the manifest and set theme color. Do not add a service worker, offline shell, precaching, or push behavior. This implements D-17, D-18, D-19, D-20, D-21, and PWA-01.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>test -f public/manifest.webmanifest && test -f public/icons/icon-192.png && test -f public/icons/icon-512.png</automated>
  </verify>
  <acceptance_criteria>
    - public/manifest.webmanifest contains `"name": "Our Private Space"`.
    - public/manifest.webmanifest contains `"display": "standalone"`.
    - public/manifest.webmanifest contains `"theme_color": "#2f4d4f"`.
    - nuxt.config.ts links `/manifest.webmanifest`.
    - No service worker file is added in public, pages, plugins, server, or root.
  </acceptance_criteria>
  <done>PWA install metadata is present without offline or push behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Document manual install and local repository checks</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/01-basic-private-space/01-CONTEXT.md, .planning/phases/01-basic-private-space/01-RESEARCH.md, .git/config</read_first>
  <action>Update `README.md` with Phase 1 verification steps: run `npm run typecheck`, run `npm run build`, sign in with seeded Firebase Auth users, refresh and confirm the session persists, log out, visit dashboard and settings, update `displayName`/`anniversaryDate`, confirm dashboard reflects the change, inspect manifest metadata in browser devtools, perform a manual Add to Home Screen check on the target device/browser, and run `git remote -v` to confirm `origin` fetch and push both point to `https://github.com/diofajrie17/chatAPP.git`. State explicitly that Phase 1 does not add service worker/offline shell behavior and does not push to GitHub unless the user asks. This implements D-21, D-22, D-23, PWA-01, and REPO-01.</action>
  <verify>
    <automated>npm run build</automated>
    <automated>git remote -v</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `Our Private Space`.
    - README.md contains `Add to Home Screen`.
    - README.md contains `git remote -v`.
    - README.md contains `https://github.com/diofajrie17/chatAPP.git`.
    - `git remote -v` output includes `origin	https://github.com/diofajrie17/chatAPP.git (fetch)` and `origin	https://github.com/diofajrie17/chatAPP.git (push)`.
  </acceptance_criteria>
  <done>README documents the PWA/manual verification path and repository-origin verification.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 1 local verification sweep</name>
  <files>README.md</files>
  <read_first>README.md, package.json, firestore.rules, public/manifest.webmanifest, .planning/phases/01-basic-private-space/01-CONTEXT.md</read_first>
  <action>Run and record the final local verification sweep in the plan summary: `npm run typecheck`, `npm run build`, `git remote -v`, and a source check that no service worker/offline shell was added. Include manual browser verification notes in the summary for login, refresh persistence, logout, dashboard, settings save, and Add to Home Screen metadata inspection. Do not push to GitHub. This closes AUTH-01, AUTH-02, DASH-01, PWA-01, REPO-01, D-17, D-18, D-21, D-22, and D-23.</action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
    <automated>git remote -v</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `npm run build` exits 0.
    - `git remote -v` prints the expected origin fetch and push URLs.
    - Repository contains no new service worker file or registration.
  </acceptance_criteria>
  <done>Phase 1 local verification commands pass and manual verification steps are documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes.
- [ ] `npm run build` passes.
- [ ] `git remote -v` shows origin fetch and push at `https://github.com/diofajrie17/chatAPP.git`.
- [ ] Browser devtools can inspect `public/manifest.webmanifest`.
- [ ] README contains the manual Add to Home Screen check.
</verification>

<success_criteria>

- PWA metadata baseline exists with manifest name `Our Private Space`.
- No service worker/offline shell behavior is added in Phase 1.
- Static icons use the approved palette and no personal imagery.
- Local GitHub origin is verified and no push is performed.
- PWA-01, REPO-01, AUTH-01, AUTH-02, DASH-01, and D-17 through D-23 are covered.
</success_criteria>

<artifacts_this_phase_produces>
- public/manifest.webmanifest
- public/icons/icon-192.png
- public/icons/icon-512.png
- Nuxt app head manifest/theme metadata
- README Phase 1 manual verification checklist
- Git remote verification evidence in summary
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/01-basic-private-space/01-04-SUMMARY.md`.
</output>
